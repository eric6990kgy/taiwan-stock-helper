# 開發日誌

*[English version](DEVLOG.md)*

從第一天到最近一次工作階段，記錄每個階段做了什麼、為什麼這樣做、學到了什麼。
`README.md` 記錄的是「現在做出來的東西」；這份文件記錄的是「怎麼走到這裡的、
過程中做了哪些決策」，包含維運工作和從沒變成正式功能、README 完全沒提到的
研究。

除非特別註明，下面每個階段都對應一次 commit。若某天的工作不是 code commit
（資料清理、研究、手動操作資料庫），會特別說明。

---

## 2026-08-28 —— V1 基礎架構：Phase 1–4

**Commit：** [`a17fa82`](../../commit/a17fa82) —— Personal Investment OS V1，Phase 1–4

一次性依照 PRD 把整套系統從零建起來：

- **資料表結構 + migration**（Phase 1）：`users`、`accounts`、`assets`、
  `transactions`、`watchlist`、`investment_thesis`、`price_history`、
  `fundamentals`，加上一個保留但目前沒有東西會寫入的 `fx_rates` 表，為未來
  多幣別支援預留。從第一天就用 Alembic——從不對正式 DB 直接
  `create_all()`。用 PRD 的示範資料集做種子（7 檔台股 + 一個全球 ETF
  基金），每一列都標記 `is_demo_data=True`/`source=MOCK`，這樣之後絕不會把
  測試假資料誤認為真實市場資料。
- **計算引擎**（Phase 2）：`app/analytics/`——成本基礎（加權平均、透過
  `replay_transactions()` 計算已實現損益）、估值、投資組合彙總。刻意讓這個
  套件完全不 import FastAPI/SQLAlchemy，用 grep 驗證——明確的目標是讓它能被
  「未來的規則/訊號引擎」重複使用而不需要重構（這個承諾正是三週後 Phase 7
  的訊號引擎和 Phase A 的風險引擎能直接插入、而不是重寫的原因）。
- **API 層**（Phase 3）：routes → services → repositories/analytics/
  providers → models。39 個 endpoint。台幣限定 + 股數不足驗證都放在 service
  層，不是 DB。所有金額都序列化成 `DecimalStr`（字串，絕不是 JS 浮點數），
  從頭到尾一致。
- **前端**（Phase 4）：6 個頁面，所有金額/百分比都透過 `decimal.js`——刻意
  讓 React 裡沒有任何財務計算。
- 包含一個 CORS 修正（允許任意 localhost 埠號，不是寫死 5173）並附上
  回歸測試——這是整個專案裡「先修好，再補上能抓到這個問題的測試」這個習慣
  第一次出現，之後還會反覆出現。

**成果：** 133 個後端測試 + 35 個前端測試，第一天就整套系統可以從瀏覽器
端到端使用。

---

## 2026-08-31 —— 真實市場資料 + 三大法人/技術面資料：Phase 5B、Phase 6

**Commit：** [`0803b31`](../../commit/0803b31)、[`65697f0`](../../commit/65697f0)

- **先做資料來源研究,再寫程式碼**：在寫任何整合之前，先比較了 TWSE/TPEx
  官方 OpenAPI、FinMind、Fugle、Yahoo Finance/yfinance、TEJ、CMoney，並寫成
  文件（[`docs/Personal_Investment_OS_Integration_Report.pdf`](docs/Personal_Investment_OS_Integration_Report.pdf)
  加上專案歷史裡的 Phase 5 Discovery Report）。**選定 FinMind**——免費方案
  可行，涵蓋 Yahoo/yfinance 完全沒有的台股專屬資料集（三大法人、融資融券、
  月營收）。明確記錄 FinMind 的條款限制*資料*（不是 client 程式碼）只能用於
  教育/非商業用途——這正是這個系統堅持保持個人/單人使用/非商業性質的硬性
  限制來源。
- **Phase 5B**：`FinMindProvider` 實作既有的 `MarketDataProvider` 介面
  （股價、基本面、股利、估值比率）——因為 Phase 3 就建好了 provider 抽象層，
  這個整合可以直接插進去，不需要動 service 或 route。新增 Settings 的
  「Update Market Data」按鈕做手動資料擷取。
- **Phase 6**：三大法人、融資融券、月營收（含年增率/月增率）、以及一個
  從零開始寫的技術指標模組（SMA/EMA/Wilder's RSI/MACD/布林通道/台灣慣例
  KD）——純函式，每個指標都驗證過不會有前視偏誤（每個 `result[i]` 只依賴
  `input[0..i]`）。Research 頁面新增 K 線圖；選股器新增外資買超/RSI/SMA20
  篩選條件。
- 另外寫了一份研究報告比較六個外部台股/量化相關 GitHub 專案，用來在動手
  做之前先校對 Phase 6 的優先順序排名。

**成果：** 281 個後端測試 + 53 個前端測試。

---

## 2026-09-07 —— Phase 6 code review：抓到並修好 8 個真實 bug

**Commit：** [`b2964d7`](../../commit/b2964d7)

一次專門針對 Phase 6 的 diff review（不是新功能）挖出了會悄悄上線的真實
bug：

- 估值 upsert 可能在缺少必要欄位時整批中途崩潰,把整次資料擷取都回滾掉。
- K 線圖會悄悄丟掉 MANUAL 來源的資料點,而不是照樣畫出來。
- 兩個**假 zero 值 bug**——`x or default` 這種寫法會把合法的 `0` 當成缺失
  資料——改用明確的 `is None` 檢查修正。（同一類 bug 兩週後又以資料問題
  （不是程式碼問題）的形式出現一次，見下面的過期 TTM 基本面資料事件。）
- `RateLimitError` 原本會被吞成一般的 `ProviderError`，導致沒有真的停止
  整批擷取——悄悄破壞了 Phase 5B 剛建立的「碰到限流就停止並回報」的契約。
- 4 個幾乎一樣的 repository 類別合併成一個共用的 `AssetDateRepository`
  base，同時修掉了 4 份程式碼裡都存在的一個未型別化 setattr 錯字漏洞。

**成果：** 289 個後端測試 + 53 個前端測試（新增 6 個針對這些 bug 的
回歸測試）。

---

## 2026-09-14 —— Phase A：風險限制、回撤斷路器、Benchmark

**Commit：** [`2a90492`](../../commit/2a90492)

**個股訊號引擎規格書**的第一塊拼圖，這天確認規格。在 `/api/analytics`
新增三個 endpoint：

- **風險限制**（`/risk`）：個股（15%）和產業（30%）集中度檢查。**刻意只
  針對個股這塊資產**——早期版本是拿整個投資組合（包含現金和全球 ETF 基金）
  來比較，結果把*刻意*配置的 65% 被動 ETF 比重標記成「集中度風險」。這完全
  搞反了：這些限制的目的是監督這個系統自己選股的行為，不是監督整個帳戶，
  而那筆 ETF 持倉正是這個系統的個股操作應該要打敗的 benchmark，不該被標記
  成問題。上線前就抓到，並加上回歸測試。
- **回撤**（`/drawdown`）：兩段式斷路器（-15% → `PAUSE_NEW_POSITIONS`、
  -25% → `HARD_STOP`），對照從既有交易紀錄 + 股價歷史回溯重建出來的權益
  曲線——不需要每日快照工作就能馬上開始使用。
- **Benchmark**（`/benchmark`）：投資組合報酬 vs. 0050 以及算出來的
  alpha——因為使用者本身已經直接持有大盤曝險，這才是個股操作真正該追求的
  成功指標。

**成果：** 28 個新測試（全部是後端/API——這個階段沒有前端變動）。

---

## 2026-09-16（上午）—— Phase 7：綜合評分、訊號引擎、LINE 通知

**Commit：** [`55494fb`](../../commit/55494fb)

- **綜合評分**：價值/成長/動能/品質子分數（0-100 或 `null`——絕不假造一個
  中性的 50 分），依市場多空判讀（TAIEX 的 SMA50/SMA200 關係 →
  BULL/BEAR/NEUTRAL）選擇對應的權重表合併子分數。每個門檻值/權重都明確
  標記為 **V1 暫定值**——方向合理，還沒經過實證驗證。（這個免責聲明後來
  非常重要——其實不是三週後，就是同一天晚上；見下面。）
- **8訊號確定性引擎**：SMA20/60、RSI、MACD（技術面）、外資/投信淨買超
  （三大法人）、營收成長正/加速（基本面）、綜合評分。即時計算，不會持久化
  儲存。`overall_status` = 不加權多數決。明確**絕不**使用「買/賣」詞彙——
  BULLISH/BEARISH 描述的是指標本身的讀數，不是指令。
- **LINE 通知**：任何訊號非中性時,搭著既有的手動「Update Market Data」
  觸發廣播一則整合訊息。執行仍然 100% 手動。

**成果：** 432 個後端測試 + 69 個前端測試。

---

## 2026-09-16（中午）—— Phase 8：風險閘門推薦引擎

**Commit：** [`ac60120`](../../commit/ac60120)

在這個系統裡直接實作外部規格書的 Phase B。看盤台（一個另外用 Claude
Artifact 做的儀表板，每次刷新都要即時重新查詢 Claude——正是這個系統改用
確定性 FinMind API 方式想避免的持續性 token 成本）被退役；它的 20 檔候選
觀察名單被拉進這個系統自己的 `assets`/`watchlist` 表
（`scripts/seed_watchlist_20.py`）。

- **策略版本化**：訊號引擎參數變成一筆版本化、可稽核的紀錄。參數名稱相同
  只是數值不同的變動會自動套用；新增或移除參數的變動會排進佇列，附帶
  walk-forward 回測前後對照，等待明確確認。
- **每日只顯示變動的掃描**：把每個觀察名單股票的狀態跟上次的快照比對——
  沒有變化的股票什麼都不會產生，解決規格書提到的「20 張沒變化的卡片洗版」
  問題。
- **建立前先過風險閘門，不是事後補救**：每一則 `CONSIDER_INCREASE` 都會
  在推薦紀錄真正寫入之前，先過 Phase A 的風險/回撤 endpoint 檢查。
- 新增 Recommendations 和 Strategy 前端頁面。

**成果：** 469 個後端測試 + 79 個前端測試。

---

## 2026-09-16（下午）—— 無人值守自動化 + 一個真實的介面 bug

**Commit：** [`cad23fd`](../../commit/cad23fd)

同時解決兩個各自獨立的問題：

1. **每天的流程還是「要記得手動點一下」。** 曾考慮在 FastAPI 裡跑一個
   in-process 排程器（APScheduler），但被否決——因為後端只有在這個對話
   session 手動啟動 dev server 時才會跑起來，排程器只會在那個時候才有效，
   完全違背「不用再記得要開著它」的初衷。改成註冊一個 **Windows 工作
   排程器**任務（`PersonalInvestmentOS-DailyUpdate`，每天 19:00——選這個
   時間是因為 FinMind 的三大法人/融資融券資料集通常這時候已經結算完畢），
   跑一個新的無人值守進入點（`backend/scripts/daily_update.py`），內部
   跟按鈕用的是完全一樣的 `MarketDataIngestionService`。每次執行都會寫入
   `backend/logs/daily_update.log`，確保無人值守時的失敗絕不會真的悄無
   聲息。
   - **上線前就抓到**：一開始很自然地用
     `logging.basicConfig(level=logging.INFO)` 設定在 root logger 上，
     結果連帶把 httpx/httpcore 自己的 request 記錄也一起抓進去——這些記錄
     包含完整的 request URL，而 `FINMIND_API_TOKEN` 正是以 URL 查詢參數
     的形式傳送的。這樣下去會導致 API token 每次執行都被寫進純文字的 log
     檔案。修正方式是改用一個具名 logger，並明確把
     `httpx`/`httpcore` 釘在 WARNING 等級，同時刪除修正前已經寫入的那份
     已經洩漏過的 log 檔案。
2. **Transactions 頁面的刪除按鈕失敗時什麼都不會顯示。** 一次被「股數
   不足」重播檢查（或任何其他 4xx 錯誤）拒絕的刪除操作會悄悄消失——沒有
   錯誤訊息，沒有任何回饋，那一列資料就是待在原地不動。修正成在該筆資料
   旁邊直接顯示真實的錯誤訊息，並附上回歸測試。

---

## 2026-09-16（晚上）—— 維運驗證：發現兩個影響正式環境的資料 bug

*沒有對應 commit——這是調查 + 直接操作資料庫，不是程式碼變更。* 實際跑了
一次「Update Market Data」並檢查 `/api/recommendations`，結果是空的。
診斷結論是**當日資料結算延遲**（FinMind 的每日資料集在收盤後才會結算；
在資料還沒公布之前就在同一天執行，本來就不會有新東西可以拿來比較）——
不是 bug，只是要正確設定時間預期。

在確認這個診斷、順手檢查底層資料的過程中，發現兩個**從 Phase 7 上線以來
一直悄悄污染 Research 和 Recommendations 頁面正式資料**的真實 bug——不只
是回測時才出現：

1. **過期的週末 `MOCK` 股價紀錄。** `seed.py` 原始的示範資料集把 11 天
   的移動視窗裡每一個*日曆*日都填滿（包含週末）；真實 FinMind 資料擷取
   只會碰真正的交易日。每個股票有兩筆週六/週日的紀錄，保留著跟兩年真實
   股價歷史完全不搭、數值離譜偏差的假資料，夾在中間。修正方式是刪除這
   14 筆過期的 `PriceHistory` 紀錄（`source='MOCK'`，那兩個週末日期），
   以及用這些資料算出來的 14 筆 `Score` 紀錄。
2. **過期的 `TTM` 標籤基本面紀錄。** `seed.py` 也幫每個示範股票插入了一筆
   假的 `Fundamentals(period="TTM", source="MOCK")` 紀錄，裡面是一個很小
   的假造營收數字。真實 FinMind 資料擷取寫入的是真正的季度期別
   （`"2023Q3"` 到 `"2026Q2"`），從來不會有 `"TTM"` 標籤的紀錄。營收成長
   的程式碼是用**字串**排序期別的——而 `"TTM"` 按字母排序會排在
   `"2026Q2"` 之*後*（`'T' > '2'`）——所以那筆假紀錄一直被當成「最新一
   期」，拿一個假造的極小數字去跟真實的最新一季比較。這導致每個示範股票
   都出現一個劃一的假 ~-90%「營收崩跌」讀數，而且從 Phase 7 上線以來就
   一直在悄悄污染 `REVENUE_GROWTH_POSITIVE`/`_ACCELERATING` 訊號和綜合
   評分裡的成長子分數。修正方式是刪除這 7 筆過期紀錄。

兩個根因追到底都是同一件事：`seed.py` 原始的假資料在這 7 檔股票有真實
資料進來之後，從來沒有被完全取代，而且沒有任何機制檢查這個落差。值得
記住：以後假資料/示範資料跟真實資料共存在同一張表裡時，要特別小心這個
模式。

這天晚上也記錄了使用者第一筆真實（非示範）交易——2408 南亞科，10 股，
均價 NT$471（原本填 NT$474，經使用者更正）——並刪除了 7 筆舊的示範交易，
只留下 2408 作為唯一的真實持倉。

---

## 2026-09-16（深夜）—— 國泰證券手續費規則、UX自動化需求、回測研究

同一天晚上還有三條沒有動到這個程式庫的支線：

- **研究了國泰證券實際的手續費規則**，並存成一條**永久記憶規則**，因為
  使用者要求之後每一筆真實交易都自動套用：手續費約為成交金額的 0.399‰
  （1.425‰ × 2.8折），最低 NT$1，買賣雙邊都算；證券交易稅 0.3% 只在
  **賣出**時收，買進絕不收。這條規則會自動套用在之後每一筆真實交易的
  記錄上，除非使用者提供實際對帳單上的數字覆蓋這個估算值。
- **回測/策略調整探索**（全部只在草稿環境進行——重複使用系統真實的
  `SignalService`/`ScoringService` 對照真實股價歷史，但從未動到這個
  程式庫本身，遵守「一次性分析,不是功能」的既定原則）：把 7 檔已有兩年
  真實回填資料的示範股票逐日重播，模擬在訊號引擎自己的 `overall_status`
  轉折點進出，每筆下單 NT$5,000，套用國泰的手續費/交易稅規則。正向規則
  （訊號亮多買進、訊號亮空賣出——照設計原樣操作）是虧錢的。反過來操作
  （訊號亮空買進、訊號亮多賣出）在每一個測試版本裡都明顯勝出：

  | 股票組合 | 規則 | 交易數 | 勝率 | 已實現損益 | 獲利因子 | 最大回撤 |
  |---|---|---|---|---|---|---|
  | 7檔示範股票 | 正向 | — | 偏低 | 負數 | <1 | 偏大 |
  | 7檔示範股票 | 反向 | — | 偏高 | 正數 | — | 3,368.60 |
  | 7檔去掉最差 | 反向 | 35 | 71.4% | +2,523.90 | 1.69 | 2,646.60 |
  | 6檔基本面篩選¹ | 正向 | 40 | 22.5% | -2,044.00 | <1 | 3,316.30 |
  | 6檔基本面篩選¹ | **反向** | 42 | **78.6%** | **+1,860.30** | **3.04** | **266.00** |

  ¹ 從 19 檔非示範真實股票裡，依 PE < 20 且連續 3 個月營收年增率為正
  篩選出來：2603 長榮、6505 台塑化、4104 佳醫、1513 中興電、2912 統一超、
  1216 統一。

  **反向操作為什麼有效——是驗證過的，不是純理論推測**：這套 8 訊號引擎
  裡短期會變動的部分（SMA/RSI/MACD）都是趨勢*確認*指標——反映的是最近
  已經發生的價格行為，不是預測未來。實際量測過：這 6 檔篩選股票在 180
  天內共 50 次轉為 BULLISH 的轉折點，轉折點*前* 5 天平均報酬是
  +1.28%（漲勢已經發生），轉折點*後* 5 天平均報酬只有 +0.08%（動能
  已經用盡）。49 次轉為 BEARISH 的轉折點，轉折點*前*平均是 -2.29%
  （已經跌深），轉折點*後*卻是 **+0.75%**（開始反彈）。在這個樣本裡，
  這些股票的短期價格行為主要是區間震盪、不是趨勢，一套趨勢確認規則
  在這種行情裡注定買在相對高點、賣在相對低點；反過來操作，等於把同一套
  訊號改造成逆勢/均值回歸的進場訊號，這正好符合這個樣本實際呈現的行情
  特徵。
  - 另外也研究了 [FinLab](https://finlab.tw) 作為第三方量化回測平台的
    可能性——結論是直接整合會造成架構上的不一致（外部付費資料依賴 vs.
    這個系統的 FinMind + 自建確定性引擎），最多只能當作未來自建回測
    模組的 API 設計參考，不適合直接整合進來。
  - **以上全部都沒有上線變成正式功能。** 樣本量偏小（42-50 筆交易）、
    6-7 檔股票彼此可能有相關性、單一 180 天的時間窗口，而且這段時間
    TAIEX 自己的多空判讀全程都是 BULL（連 regime 過濾這個假設都沒辦法
    在這個樣本裡真正被檢驗）。如果之後真的要把這個發現變成正式的策略
    版本，需要使用者明確做一次決定,並重新走一次規劃流程——不能只靠
    一次回測的結果就推論。

---

## 2026-09-17 —— Phase 12：Gemini 多代理人研究團隊

**Commit：** [`7f32326`](../../commit/7f32326)

你要求做一個模仿真實券商研究部門分工的「團隊」——基本面研究員、技術面
研究員、負責綜合判斷的角色——並且要有一套 KPI 制度，讓你分得出哪個
agent 的意見真正值得聽。你選了 Gemini 而不是 Claude（理由是你在 Google
Antigravity 已經有一套熟悉的多代理人人格系統），所以這個階段只做
Gemini。

從第一個階段沿用到現在、完全沒變的規則：**決定性的 Phase 8 引擎依然掌握
真正的決定。** 這裡沒有任何東西讓 LLM 改變 `action`/`new_status`/
`risk_blocked`——三個 Gemini agent 產生的是附掛在既有 `Recommendation`
旁邊的研究意見，不是取代它。

- **動手寫程式碼之前先驗證 SDK 用法，跟 `claude-api` skill 對 Claude API
  一直堅持的紀律一樣。** 一開始的計畫假設要用 `client.interactions.create()`——
  結果那其實是另一套完全不同、以 session/agent 為中心的介面（持久化的
  Agent、webhook、trigger），不適合一次性的結構化輸出呼叫。實際在本機
  安裝 `google-genai` 並檢查真正的 client 之後才動筆寫
  `app/services/gemini_client.py`——正確的介面其實是
  `client.models.generate_content()` 搭配 Pydantic 的 `response_schema`。
  這個錯誤在寫任何正式程式碼*之前*就被抓到，不是事後才發現。
- **`AgentResearchService`**（`app/services/agent_research_service.py`）：
  基本面研究員跟技術面研究員直接沿用 `ResearchService` 既有的資料存取
  方法（不新增查詢）；投資組合經理人額外看得到另外兩個角色自己的輸出，
  加上決定性引擎已經做出的決定，當成固定的背景資訊——它可以評論或不同意
  系統的判斷，但它自己的輸出永遠不會變成系統的判斷。每個角色各自獨立
  失敗（`_safe_role`），一個角色回應失敗絕不會連帶讓另外兩個也不見。
- **速率限制，是實際跑出來才發現的，不是事先預料到的。** 使用者開通
  付費帳單之後的第一次真實掃描，還是全部以 `429`/`503` 失敗——Gemini
  免費層把 `gemini-3.8-flash` 限制在每分鐘 5 次請求，而一次掃描只要有
  幾檔股票訊號變化 × 3 個角色，一次爆發性送出就會直接超過這個上限。
  後來確認只是帳單生效有延遲，帳單真正生效之後就正常了——但不管怎樣，
  還是在 `GeminiClient` 加了真正的重試機制（先等 5 秒、再等 15 秒，只對
  `429`/`503` 重試，`400`/認證錯誤不重試），因為即使是付費層，偶發性
  限流仍然可能再發生。
- **一個真的會影響到之後每次測試的隔離性 bug。** 一旦本機 `.env` 裡有
  真的 `GEMINI_API_KEY`（現在這台開發機上會永久存在），任何呼叫
  `MarketDataIngestionService.update_all()` 卻沒有明確替換掉
  `agent_research_service` 的測試，都會在跑 `pytest` 時真的打出去、真的
  被計費——不管是 API 測試還是原本完全沒預期會有真金鑰存在的既有單元
  測試都會中獎。FinMind 那邊早就靠一個依賴注入介面
  （`get_finmind_provider`，每個測試用 `StubProvider` 覆寫）解決過一樣的
  問題——Gemini client 完全沒有對應的介面。用兩層修法解決：在
  `app/api/deps.py` 加一個 `get_gemini_client()` 介面，串進
  `MarketDataIngestionService.__init__`；另外因為單元測試是直接建構這個
  service、完全跳過 API 層的依賴注入，所以又在 `tests/conftest.py`
  （這個檔案在 `tests/unit/` 和 `tests/api/` 兩邊之上）加了一個
  `autouse=True` 的全域 fixture，直接把
  `app.services.gemini_client.GEMINI_API_KEY` patch 成 `None`，對每個
  測試一律生效。值得記住：之後這個專案任何新的外部 API 整合，從第一天
  就要準備好這兩層介面，不要等到真金鑰開始在每次測試偷偷花錢之後才補。
- **使用者看到真實內容之後，輸出格式整個改掉。** 第一次真實掃描產生的
  分析內容品質其實很好，但每個角色只有一段自由發揮的 `rationale`
  長文字，讀起來像一整篇文章，不像報表。改成三個角色各自的 Pydantic
  schema（`FundamentalCallSchema`/`TechnicalCallSchema`/
  `PortfolioManagerCallSchema`），每個都是 `call`/`confidence` 加上
  3-4 個固定、有標籤的一句話欄位（例如基本面研究員的
  `revenue_trend`/`profitability`/`cash_flow_and_balance`/`key_risk`）。
  `AgentAnalysis.rationale: Text` 改成 `details: JSON`
  （migration `40ca7918d59f`）——改版前用舊格式產生的 18 筆資料直接
  丟棄重新產生,而不是寫轉換腳本,因為那些都是同一天的 AI 產出、重新生成
  成本很低（約 $0.05 美元），不值得為了保留它們寫一次性的資料轉換。
- **KPI 直接沿用 Phase 10 的資料，沒有另外做一次評分**
  （`app/services/agent_performance_service.py`）：每個角色的 `call`
  拿去跟決定性引擎自己的 call 已經在比對的同一個
  `RecommendationOutcome.actual_direction` 比對——`UNAVAILABLE` 的判斷
  排除在分母之外,跟 Phase 10 排除 `WATCH`（不具方向性資訊）用的是同一個
  原則。
- **Recommendations 頁**的展開列新增了每個角色的結構化卡片（判斷徽章、
  信心度、標籤欄位、`key_risk` 用紅色標示）；**Review 頁**新增「Agent
  Performance」區塊，每個角色一張命中率卡片，跟上面決定性引擎的卡片
  一樣，樣本不足就老實顯示「insufficient sample」。
- **新增 24 個後端測試**（512 → 536）,前端既有測試改寫以符合新的卡片
  格式（總數維持 113 個，是改寫既有案例,不是新增檔案）。兩個新的
  Alembic migration（`a3ea98c9cdec` 新增 `agent_analyses`；
  `40ca7918d59f` 把 `rationale` 改成 `details`）都在乾淨的 DB 上驗證過。
- **刻意不讓 LLM 碰**：風控/合規維持既有的 Phase A 規則引擎不變——讓 LLM
  把關財務決定，會重新引入這整個專案從第一個階段就刻意設計避開的幻覺
  風險。

---

## 尚待規劃的事項

- **持續學習迴圈**（投資日誌/回饋報告/複盤——一個檢視過去推薦跟實際結果
  對照的回饋機制）——已經提出需求，還沒設計或動工。
- **2408 自己的回測** —— 整個工作階段都卡在 FinMind 速率限制上；截至
  目前這個股票在本機仍然沒有股價歷史資料。7 檔示範股票和 6 檔篩選股票
  正是因為本機已經有兩年真實回填資料，才被拿來當替代品使用。
- **要不要把反向/均值回歸這個發現正式化**成一個真正的 `StrategyVersion`
  —— 使用者還沒有做這個決定，上面特別標註這只是草稿環境的探索是有原因的。
